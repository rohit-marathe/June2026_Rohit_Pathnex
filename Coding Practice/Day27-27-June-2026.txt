# Day - 27

# Ansible - Copy Directory Recursively 

---
- name: Copy Pathnex Files
  hosts: all
  become: yes

  tasks:
    - name: Copy Directory
      copy:
        src: /opt/pathnex
        dest: /backup/pathnex
        remote_src: yes

# Terraform - EBS Volume + Attachment

resource "aws_ebs_volume" "PathnexVolume" {
    availability_zone = "us-east-1a"
    size = 20

    tags = {
        Name = "Pathnex-Volume"
    }
}

resource "aws_volume_attachment" "PathnexAttach" {
    device_name = "/dev/sdh"
    volume_id = aws_ebs_volume.PathnexVolume.id 
    instance_id = aws_instance.PathnexEC2.id
}

# Kubernetes - NodePort Service

apiVersion: v1
kind: Service
metadata:
    name: pathnex-nodeport 
spec:
    type: NodePort
    selector:
        app: pathnex 
    ports:
        - port: 80
          targetPort: 80
          nodePort: 30680

# Shell Script - Clean Old Logs

#!/bin/bash 

find /var/log -type -f -mtime +7 -exec rm -f {} \;

echo "Old logs removed"

# Jenkins Scripted Pipeline - Complex Logic 

node {
    def envName = "dev"
    env.INSTITUTE_NAME = "Pathnex"
    env.TEAM = "DevOps"

    stage('Checkout') {
        git url: 'completed assignment of 26th june - L1+L2'
    }
    stage('Build') {
        if (envName == "dev") {
            sh 'echo "Building $INSTITUTE_NAME app in dev environment for $TEAM"'
        } else {
            sh 'echo "Skipping Build for $envName"'
        }
    }
    stage('Deploy') {
        input message: "Approve Deployment for $envName?"
        sh 'eho "Deploying $INSTITUTE_NAME app to $envName for $TEAM team"'
    }
}


# L2 - Coding Practice 


# Ansible - Deploy Serverless Lambda Function

- name: Deploy Serverless Lambda Function
  hosts: localhost
  tasks:
    - name: Create Lambda function 
      command: aws lambda create-function --function-name pathnex-lambda --runtime nodejs14.x -role arn:aws:iam:123456789012:role/lambda-role --handler index.handle --zip-file fileb://lambda.zip


# Terraform - Create DynamoDB Table for Serverless Application 

resource "aws_dynamodb_table" "pathnex_table" {
    name = "pathnex-table"
    hash_key = "id"
    billing_mode = "PAY_PER_REQUEST"
    
    attribute {
        name = "id"
        type = "S"
    }
}

# Kubernetes - Deploy Serverless Using Kubless

apiVersion: kubeless.io/v1beta1
kind: Function
metadata:
    name: pathnex-lambda
    namespace: default
spec:
    runtime: nodejs14
    handler: index.handler
    function: |
        module.exports = function(event, context) {
            context.succeed('Hello World');
        };

# Jenkinsfile - Lambda Function Deployment Pipeline 

pipeline {
    agent any 
    stages {
        stage('Build') {
            steps {
                echo 'Building Lambda Function Package...'
                sh 'zip -r lambda.zip lambda/'
            }
        }
        stage('Deploy to Lambda') {
            steps {
                scritp {
                    sh 'aws lambda update-function-code --function-name pathnex-lambda --zip-file fileb://lambda.zip'
                }
            }
        }
    }
}

# Docker - Docker Exec & Resource Limits

docker exec -it pathnex-nginx bash 
docker stats

# Bash 

docker run -d --memory="512m" --cpus="1.0" nginx

