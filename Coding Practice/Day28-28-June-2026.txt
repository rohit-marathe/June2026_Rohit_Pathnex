# Day - 28 

# Ansible - Fixed

- name: Install nginx
  hosts: all
  become: yes

  tasks:
    - name: Instal nginx package
      yum:
        name: nginx
        state: present 

#Fixes made:

#Added missing : after name and hosts
#Changed hosts all → hosts: all
#Fixed indentation
#Added a meaningful task name
#Added become: yes (commonly required for package installation)

# Terraform - Fixed

resource "aws_s3_bucket" "bad_bucket" {
    bucket = "pathnex-bucket"
    acl = "public-read"
}

#Fixes made:

#Correct resource type: aws_s3_bucket
#Resource name changed to lowercase with underscore (bad_bucket)
#Bucket name enclosed in quotes
#acl assigned with =
#Proper formatting and indentation

#Note: In newer AWS provider versions, ACLs are often managed separately using aws_s3_bucket_acl.

# Kubernetes - Fixed 

apiVersion: v1
kind: Pod 

metadata:
    name: brokenpod

spec:
    containers:
        - name: app
          image: nginx 

#Fixes made:

#Added missing : after apiVersion and kind
#Corrected indentation
#Fixed -name → - name
#Fixed image nginx → image: nginx


# Jenkins Pipeline - Automatic Rollback

pipeline {
    agent any

    environment {
        INSTITUTE_NAME = "Pathnex"
        TEAM = "DevOps"
        ENV = "prod"
    }
    stages {
        stage('Deploy') {
            steps {
                script {
                    try {
                        sh 'echo "Deploying $INSTITUTE_NAME app in $ENV for $TEAM"'
                        sh 'exit 1' // Simulate deployment failure.
                    } catch (Exception e) {
                        echo "deployment failed. Rolling back $INSTITUTE_NAME app in $ENV"

                        // Rollback command
                        sh 'echo "Rollback Completed."'

                        currentBuild.result = 'FAILURE'
                    }
                }
            }
        }
    }
}


# Gitlab CI - Rollback Strategy 

stages:
    - deploy 

variables:
    INSTITUTE_NAME: "Pathnex"
    TEAM: "DevOps"
    ENV: "prod"

deploy:
    stage: deploy
    image: alpine:latest 

script:
    - echo "Deploying $INSTITUTE_NAME app in $ENV for $TEAM"
    - exit 1

after_script:
    - echo "Deployment failed. Rolling back $INSTITUTE_NAME app in $ENV"
    - echo "Rollback completed."

#Fixes made:

#Corrected variable syntax:

#TEAM: "DevOps"
#ENV: "prod"

#instead of

#TEAM = "DevOps"
#ENV = "prod"
#Improved formatting and indentation
#Added a simulated rollback completion message


# L2 - Coding Practice 

# Ansible- Set Up VPC Peering Between AWS Accounts

- name: Setup VPC Peering Between Accounts
  hosts: localhost
  tasks:
    - name: Create VPC peering connection 
      command: aws ec2 create-vpc-peering-connection --vpc-id vpc-12345678 --peer-vpc-id vpc-87654321

# Terraform - VPC Peering And Route Tables

resource "aws_vpc_peering_connection" "pathnex_peering" {
    vpc_id = aws_vpc.pathnex_vpc.id
    peer_vpc_id = aws_vpc.peer_vpc.id 
}

resource "aws_route" "pathnex_route" {
    route_table_id = aws_route_table.pathnex_rt.id
    destination_cidr_block = "10.0.0.0/16"
    aws_vpc_peering_connection.id = aws_vpc_peering_connection.pathnex_peering.id
} 

# Kubernetes - Multi-Cluster VPC Peering and Network Policy

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
    name: pathnex-vpc-policy 

spec:
    podSelector:
        matchLabels:
            app: pathnex-app
    ingress:
        - form: 
            - podSelector:
                matchLabels:
                    app: pathnex-db 

# Docker - Docker Save, Load & Backup 

docker save -o pathnex-nginx.tar pathnex-nginx

docker load -i pathnex-nginx.tar 

# Bash

docker run -rm -v pathnex-volume:/data - v $(pwd):/backup ubuntu tar czf /backup pathnex-backup.tar.gz /data

